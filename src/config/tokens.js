const tokenTypes = {
  ACCESS: 'access',
  REFRESH: 'refresh',
};

const cartItemStatuses = {
  ACCEPT: 'accept',
  PENDING: 'pending',
  PROCESS: 'process',
};

module.exports = {
  tokenTypes,
  cartItemStatuses,
};
