const genders = {
  MALE: 'male',
  FEMALE: 'female',
};

const teacherStatuses = {
  PENDING: 'pending',
  ACCEPTED: 'accepted',
  REJECTED: 'rejected',
  BLOCKED: 'blocked',
};

module.exports = {
  genders,
  teacherStatuses,
};
